apiVersion: apps/v1
kind: Deployment
metadata:
  name: summit-fabric
  labels:
    app: summit-fabric
spec:
  replicas: 1
  selector:
    matchLabels:
      app: summit-fabric
  template:
    metadata:
      labels:
        app: summit-fabric
    spec:
      containers:
        - name: fabric
          image: ghcr.io/bigmt/summit-fabric:latest
          ports:
            - containerPort: 8001
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          env:
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: summit-config
                  key: REDIS_URL
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: summit-secrets
                  key: POSTGRES_PASSWORD
            - name: FABRIC_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: summit-secrets
                  key: FABRIC_JWT_SECRET
            - name: POSTGRES_URL
              value: "postgresql+asyncpg://summit:$(POSTGRES_PASSWORD)@summit-postgres:5432/summit_os"
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: summit-fabric
spec:
  selector:
    app: summit-fabric
  ports:
    - name: http
      port: 8001
      targetPort: 8001
