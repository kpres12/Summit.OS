apiVersion: apps/v1
kind: Deployment
metadata:
  name: summit-fusion
  labels:
    app: summit-fusion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: summit-fusion
  template:
    metadata:
      labels:
        app: summit-fusion
    spec:
      containers:
        - name: fusion
          image: ghcr.io/bigmt/summit-fusion:latest
          ports:
            - containerPort: 8002
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
          env:
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: summit-config
                  key: REDIS_URL
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: summit-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_URL
              value: "postgresql+asyncpg://summit:$(POSTGRES_PASSWORD)@summit-postgres:5432/summit_os"
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: summit-fusion
spec:
  selector:
    app: summit-fusion
  ports:
    - name: http
      port: 8002
      targetPort: 8002
