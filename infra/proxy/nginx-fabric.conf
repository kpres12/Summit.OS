# NGINX mTLS front proxy for Fabric service
worker_processes  1;

events { worker_connections 1024; }

http {
  upstream backend_fabric { server fabric:8001; }

  # Extract org_id from OU field in client certificate
  map $ssl_client_s_dn $org_id {
    default "";
    ~OU=(?<ou>[^/]+) $ou;
  }

  server {
    listen 8443 ssl;
    server_name summit-fabric;

    ssl_certificate     /certs/server.crt;
    ssl_certificate_key /certs/server.key;
    ssl_client_certificate /certs/ca.crt;
    ssl_verify_client   on;
    ssl_verify_depth    2;

    log_format mtls_access '$remote_addr - $org_id [$time_local] '
                           '"$request" $status $body_bytes_sent';
    access_log /var/log/nginx/access.log mtls_access;

    location / {
      if ($org_id = "") {
        return 403 "Client certificate must include OU field for org_id";
      }

      proxy_set_header X-Client-DN $ssl_client_s_dn;
      proxy_set_header X-Org-ID $org_id;
      proxy_set_header X-Client-Cert-Verified $ssl_client_verify;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_pass http://backend_fabric;
    }

    location /health {
      proxy_pass http://backend_fabric/health;
    }
  }
}
