global:
  image:
    registry: ""
    pullPolicy: IfNotPresent
  env:
    REDIS_URL: redis://redis:6379
    POSTGRES_URL: postgresql://heli:summit@postgres:5432/summit
    MQTT_BROKER: mqtt://mqtt:1883

# ── Secrets (CHANGE_ME before deploying) ────────────────────
secrets:
  postgresPassword: "CHANGE_ME"
  fabricJwtSecret: "CHANGE_ME"
  fieldEncryptionKey: "CHANGE_ME"  # openssl rand -base64 32
  stripeWebhookSecret: ""
  grafanaAdminPassword: "CHANGE_ME"

# ── Runtime config (rendered into ConfigMap) ────────────────
config:
  apiKeyEnforce: "false"
  oidcEnforce: "false"
  otlpEndpoint: "http://jaeger:4317"
  # Production overrides:
  # oidcIssuer: "https://your-keycloak/realms/heli"
  # oidcAudience: "summit-api"
  # corsOrigins: "https://console.yourdomain.com"
  # ollamaUrl: "http://ollama:11434"
  # ollamaModel: "llama3.1"

# ── API Gateway ─────────────────────────────────────────────
apiGateway:
  replicas: 2
  image: heli-os/api-gateway:latest
  port: 8000
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 512Mi }
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilization: 70

# ── Fabric Service ──────────────────────────────────────────
fabric:
  replicas: 2
  image: heli-os/fabric:latest
  port: 8001
  resources:
    requests: { cpu: 200m, memory: 256Mi }
    limits: { cpu: 1000m, memory: 1Gi }

# ── Fusion Service ──────────────────────────────────────────
fusion:
  replicas: 2
  image: heli-os/fusion:latest
  port: 8002
  resources:
    requests: { cpu: 500m, memory: 512Mi }
    limits: { cpu: 2000m, memory: 2Gi }

# ── Intelligence Service ────────────────────────────────────
intelligence:
  replicas: 2
  image: heli-os/intelligence:latest
  port: 8003
  resources:
    requests: { cpu: 500m, memory: 512Mi }
    limits: { cpu: 2000m, memory: 4Gi }
  env:
    MODEL_BACKEND: "auto"

# ── Tasking Service ─────────────────────────────────────────
tasking:
  replicas: 2
  image: heli-os/tasking:latest
  port: 8004
  resources:
    requests: { cpu: 200m, memory: 256Mi }
    limits: { cpu: 1000m, memory: 1Gi }

# ── Inference Service ───────────────────────────────────────
inference:
  replicas: 1
  image: heli-os/inference:latest
  port: 8006
  resources:
    requests: { cpu: 500m, memory: 1Gi }
    limits: { cpu: 2000m, memory: 4Gi }
  env:
    INFERENCE_BACKEND: "auto"

# ── Console (Frontend) ─────────────────────────────────────
console:
  replicas: 2
  image: heli-os/console:latest
  port: 3000
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 512Mi }

# ── Infrastructure ──────────────────────────────────────────
redis:
  enabled: true
  architecture: standalone
  auth: { enabled: false }
  resources:
    requests: { cpu: 100m, memory: 128Mi }

postgres:
  enabled: true
  auth:
    postgresPassword: summit
    database: summit
  primary:
    resources:
      requests: { cpu: 250m, memory: 256Mi }
    persistence:
      size: 10Gi

mqtt:
  enabled: true
  image: eclipse-mosquitto:2
  resources:
    requests: { cpu: 100m, memory: 64Mi }

# ── Observability ───────────────────────────────────────────
prometheus:
  enabled: true
  server:
    resources:
      requests: { cpu: 200m, memory: 256Mi }
    retention: 30d

grafana:
  enabled: true
  adminPassword: summit
  resources:
    requests: { cpu: 100m, memory: 128Mi }

# ── Mesh Network ────────────────────────────────────────────
mesh:
  port: 9100
  encryption: true

# ── Security ────────────────────────────────────────────────
security:
  mtls:
    enabled: true
    certValidityDays: 365
  jwt:
    issuer: heli-os
    tokenTTL: 3600

# ── Ingress ─────────────────────────────────────────────────
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: summit.local
      paths:
        - path: /
          service: console
        - path: /api
          service: apiGateway
