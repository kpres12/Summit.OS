# External provider configuration for Summit.OS

# Security (CRITICAL: Change these in production!)
POSTGRES_PASSWORD=summit_password_changeme
FABRIC_JWT_SECRET=dev_secret_changeme
GRAFANA_ADMIN_PASSWORD=admin_changeme

# OIDC (Auth)
NEXT_PUBLIC_OIDC_ISSUER=https://your-issuer.example.com/realms/yourrealm
NEXT_PUBLIC_OIDC_CLIENT_ID=summit-console
NEXT_PUBLIC_OIDC_REDIRECT_URI=http://localhost:3000/auth/callback
OIDC_CLIENT_SECRET=

# API Gateway OIDC enforcement (optional)
OIDC_ENFORCE=false
OIDC_ISSUER=https://your-issuer.example.com/realms/yourrealm
OIDC_AUDIENCE=summit-api
OIDC_JWKS_URL=https://your-issuer.example.com/realms/yourrealm/protocol/openid-connect/certs

# Policy (OPA)
OPA_URL=http://opa.yourdomain.com:8181

# Messaging (MQTT)
MQTT_BROKER=mqtt.yourdomain.com
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# Caches/Streams
REDIS_URL=redis://redis.yourdomain.com:6379

# Databases
POSTGRES_URL=postgresql://user:password@db.yourdomain.com:5432/summit_os

# API wiring for API Gateway (use external services if provided)
FABRIC_URL=http://fabric.yourdomain.com:8001
FUSION_URL=http://fusion.yourdomain.com:8002
INTELLIGENCE_URL=http://intelligence.yourdomain.com:8003
TASKING_URL=http://tasking.yourdomain.com:8004

# Console
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8001

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ─── Spatial Intelligence Stack ─────────────────────────────

# OpenSky Network (live aircraft ADS-B feed)
OPENSKY_ENABLED=true
OPENSKY_POLL_INTERVAL=10
OPENSKY_BBOX=                         # lat_min,lon_min,lat_max,lon_max (empty=worldwide)
OPENSKY_USERNAME=
OPENSKY_PASSWORD=

# CelesTrak (satellite orbital positions)
CELESTRAK_ENABLED=true
CELESTRAK_MAX_SATS=200
CELESTRAK_PROPAGATE_INTERVAL=30
CELESTRAK_GROUP=active

# CesiumJS 3D Globe (Cesium Ion token for terrain/imagery — free at cesium.com)
NEXT_PUBLIC_CESIUM_TOKEN=

# SOC2 / Security
FIELD_ENCRYPTION_KEY=              # base64-encoded 32-byte key: openssl rand -base64 32
RBAC_ENFORCE=false                 # Set true in production to enforce JWT role claims
AUDIT_RETENTION_DAYS=90            # Days to retain audit log entries

# Alertmanager (optional — configure webhooks for notifications)
ALERTMANAGER_WEBHOOK_URL=
ALERTMANAGER_CRITICAL_WEBHOOK_URL=
ALERTMANAGER_SECURITY_WEBHOOK_URL=
PAGERDUTY_INTEGRATION_KEY=
SLACK_WEBHOOK_URL=

# Stripe Billing
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID_PRO=price_...
STRIPE_PRICE_ID_ENTERPRISE=price_...

# API key enforcement (set true in production)
API_KEY_ENFORCE=false

# Dev mode flags
UVICORN_RELOAD=false
