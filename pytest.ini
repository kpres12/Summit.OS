[pytest]
addopts = -q
markers =
    e2e: end-to-end tests requiring live services
    integration: integration tests
    unit: unit tests
    perf: performance benchmarks (opt-in: run explicitly with -m perf)
